extends ./layout.jade
block title
  title= title

block content
  h1 #{title}
  div.container
    form(action="/room/" + token, method="post")
      textarea(name="room_name_hash") #{room_url}
      input(type="submit", value="ENTER")
      input(type="hidden", value=token)